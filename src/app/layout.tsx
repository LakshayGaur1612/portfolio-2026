import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Lakshay Gaur — Product Designer",
  description: "Portfolio 2026: enterprise SaaS & AI case studies, personal experiments, and playground artefacts.",
  openGraph: {
    title: "Lakshay Gaur — Product Designer",
    description: "Enterprise SaaS & AI portfolios, 5+ yrs. 2-Wheeler, BioBrain, Habitat, Whyser.",
    type: "website",
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
